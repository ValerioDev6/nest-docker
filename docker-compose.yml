version: '3.1'

services:
  acasio_pg:
    image: postgres:15.1
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    container_name: acasio_pg
    volumes:
      - ./postgres:/var/lib/postgresql/data
